global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "fraud-drift-monitoring"
    static_configs:
      - targets: ["monitoring:9200"]

  # Когда коллега добавит /metrics в FastAPI-сервис — раскомментировать
  # и поставить правильное имя контейнера/порт из его docker-compose блока.
  # - job_name: "fraud-api"
  #   static_configs:
  #     - targets: ["api:8000"]
